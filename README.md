# CV en ligne - Thème macOS

Un CV interactif original avec un design inspiré de macOS, construit avec Nuxt 3.

## 🚀 Démarrage

```bash
# Installer les dépendances
npm install

# Lancer le serveur de développement
npm run dev

# Build pour la production
npm run build

# Prévisualiser la build de production
npm run preview
```

## 📁 Structure du projet

- `pages/` - Pages de l'application (index, locked, home)
- `components/` - Composants réutilisables
- `assets/css/` - Styles CSS globaux
- `public/` - Fichiers statiques (images, etc.)

## 🎨 Fonctionnalités

- Page de démarrage avec animation Apple
- Page de connexion avec authentification
- Bureau macOS interactif avec :
  - Barre de menu
  - Widgets (calendrier, météo)
  - Icônes de dossiers déplaçables
  - Fenêtre Finder avec navigation
  - Dock avec icônes d'applications

## 🔐 Mot de passe par défaut

Le mot de passe par défaut est : `azerty`



